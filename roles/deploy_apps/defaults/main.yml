---
# App deployment defaults
apps_base_dir: "/opt"
apps_user: "{{ server_user }}"
apps_group: "{{ server_user }}"

# Git configuration
git_force_update: true
git_accept_hostkey: true

# Docker Compose configuration
compose_command: "docker compose -p {{ app_name }}"
compose_project_directory_suffix: ""

# Docker management
cleanup_docker_images: true
force_rebuild: false
build_timeout: 1200 # 20 minutes
no_cache: false

# Network configuration
proxy_network_name: "proxy"

# Cleanup and retention configuration
releases_to_keep: 5
backups_to_keep: 5
